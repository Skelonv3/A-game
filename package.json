{
  "name": "skelonv3_a-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:start": "mkdir -p ./.data/postgres && docker compose up -d",
    "docker:stop": "docker compose down --remove-orphans",
    "docker:app": "docker compose exec -it app bash",
    "docker:server": "docker compose exec -it server bash",
    "docker:db": "docker compose exec -it db bash",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.34.1",
    "pg": "^8.13.0",
    "pg-pool": "^3.7.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.25.0",
    "tsx": "^4.19.1"
  }
}
